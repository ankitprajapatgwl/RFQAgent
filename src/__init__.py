"""RFQ Agent platform package.

This package hosts the RFQ Agent application. The current module implements the
authentication feature (user registration and login) exposed both as a JSON API
and as server-rendered HTML pages, following the layered architecture described
in ``coding_standards_documents/``.
"""

__version__ = "0.1.0"
