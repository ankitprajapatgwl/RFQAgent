"""Domain layer — framework-agnostic core types (ORM models, enums, schemas).

This layer holds the business vocabulary of the authentication feature. It has
no FastAPI imports, so it can be reused and unit-tested independently of the
HTTP layer.
"""
